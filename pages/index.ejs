<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">
    <title><%= title %> </title>
</head>

<body>
    <% if(created) { %>
        <div class="alert alert-success">
            Note has been created!
        </div>
        <% } %>  
 <div class="container">
    <h1>HERE</h1>
    <form action="/" method="post" class="mb-2">
        <div class="from-group mb-2">
            <input type="text" class="form-contol" required name="title">
        </div>
        <button class="btn btn-primary" type="submit">Create!</button>
    </form>
    <% if(notes.length) { %> 
    <div class="card">
        <form action="/" method="put"  >
        <ul class="list-group ">
            <% for (let i = 0; i < notes.length; i++)  { %> 
              
            <li class="list-group-item d-flex justify-content-between">
                <div class="textBlock">
                    <%= notes[i].title %> 
                </div>
                <input class="textInp" style="display: none;" required value="<%= notes[i].title %>" name="title"/>
                  
              
                <div class="blockBtn">
                    <button class="btn btn-primary update" data-type='update' type="button" data-id="<%= notes[i].id %> ">Update</button>
                    <button class="btn btn-danger remove" data-type='remove' type="button" data-id="<%= notes[i].id %> ">&times;</button>
                    <button class="btn btn-success Cancel" data-type='save' type="submit" data-index = '<%= i %>' style="display: none;">Save</button>
                    <button class="btn btn-danger Cancel" data-type='cancel' type="button" style="display: none;" >Cancel</button>
                </div>
            </li>
        
            <% } %> 
        </ul>
    </form>
    </div>
  <% } else { %> 
    <p>Notes doesnt exsist </p>
    <% } %> 
 </div>
 <script src="/app.js"></script>
</body>

</html>



